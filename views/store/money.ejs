<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x"
      crossorigin="anonymous"
    />
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="store_UI/assets/favicon_io/apple-touch-icon.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="store_UI/assets/favicon_io/favicon-16x16.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="store_UI/assets/favicon_io/favicon-32x32.png"
    />
    <link rel="manifest" href="store_UI/assets/favicon_io/site.webmanifest" />
    <link rel="stylesheet" href="store_UI/style.css" />
    <title>Payment</title>
    <style>
      .container {
        width: 768px;
      }

      #payment_selector {
        width: 144px;
      }

      .col-form-label {
        width: 160px;
        font-weight: 500;
      }

      .table-outline {
        box-shadow: rgb(0 0 0 / 15%) 0px 4px 4px -2px;
      }

      .table {
        box-shadow: 0 0 2px rgb(0 0 0 / 12%);
      }

      .paymentSS {
        width: 300px;
      }

      #viewModal .modal-dialog {
        max-width: 900px;
      }

      #viewModal .modal-body .col-9 {
        min-height: 200px;
      }

      .payment_screenshot {
        height: 200px;
        width: 200px;
      }
    </style>
    <script
      src="https://kit.fontawesome.com/4d8145f93f.js"
      crossorigin="anonymous"
    ></script>
  </head>

  <body>
    <!-- Hamburger menu with back link on right side-->
    <div class="container my-2">
      <div class="row heading">
        <label for="payment_selector" class="col-sm-2 col-form-label"
          >Type of Payments</label
        >
        <select
          class="form-select"
          aria-label="Select Payment Type"
          id="payment_selector"
        >
          <option selected value="pending">Pending</option>
          <option value="completed">Completed</option>
        </select>
      </div>
      <div class="row content mt-4 py-1">
        <div class="pending_table">
          <div class="table-outline">
            <table class="table table-hover">
              <thead>
                <tr>
                  <th scope="col">S.No.</th>
                  <th scope="col">Product Details</th>
                  <th scope="col">Date</th>
                  <th scope="col">C.P.</th>
                </tr>
              </thead>
              <tbody>
                <% for(let i=0;i < pendingMoneyDetails.length; i++) {%>
                <tr>
                  <th scope="row"><%=(i+1)%></th>
                  <td><%=pendingMoneyDetails[i].productName%></td>
                  <td>
                    <%= pendingMoneyDetails[i].orderTime.getDate() + '/' +
                    pendingMoneyDetails[i].orderTime.getMonth()+ '/'+
                    pendingMoneyDetails[i].orderTime.getFullYear()%>
                  </td>
                  <td><%= pendingMoneyDetails[i].costPrice%></td>
                </tr>
                <% } %>
              </tbody>
              <tfoot>
                <tr>
                  <th scope="row" colspan="3">Total</th>
                  <th><%= unPaidAmount %></th>
                </tr>
              </tfoot>
            </table>
          </div>
        </div>
        <div class="completed_table d-none">
          <div class="table-outline">
            <table class="table table-hover">
              <thead>
                <tr>
                  <th scope="col">S.No.</th>
                  <th scope="col">Payment No.</th>
                  <th scope="col">Payment Date</th>
                  <th scope="col">Details</th>
                </tr>
              </thead>
              <tbody>
                <% for(let i=0;i < payments.length;i++) { %>
                <tr>
                  <th scope="row"><%=(i+1)%></th>
                  <td><%=payments[i].paymentNumber%></td>
                  <td>
                    <%= payments[i].paymentTime.getDate() + '/' +
                    payments[i].paymentTime.getMonth()+ '/'+
                    payments[i].paymentTime.getFullYear()%>
                  </td>
                  <td>
                    <a
                      href=""
                      class="modalButton"
                      data-bs-toggle="modal"
                      data-bs-target="#viewModal"
                      data-number="<%=i%>"
                      data-image="<%=payments[i].paymentProof.data.toString('base64')%>"
                      data-payment="<%=paymentsJSON%>"
                      data-paymentdetails="<%=paymentDetails%>"
                      >View Details</a
                    >
                  </td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

    <!-- Modal for viewing details -->
    <div
      class="modal fade"
      id="viewModal"
      data-bs-backdrop="static"
      data-bs-keyboard="false"
      tabindex="-1"
      aria-labelledby="viewModalLabel"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">Details</h5>
            <button
              id="removeElements"
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body">
            <div class="row">
              <div class="col-9">
                <div class="table-outline">
                  <table class="table table-hover">
                    <thead>
                      <tr>
                        <th scope="col">S.No.</th>
                        <th scope="col">Product Details</th>
                        <th scope="col">Date</th>
                        <th scope="col">C.P.</th>
                        <th scope="col">Quantity</th>
                      </tr>
                    </thead>
                    <tbody id="tableBody"></tbody>
                    <tfoot>
                      <tr>
                        <th scope="row" colspan="3">Total</th>
                        <th id="totalCP"></th>
                      </tr>
                    </tfoot>
                  </table>
                </div>
              </div>
              <div class="col-3">
                <img
                  id="paymentProof"
                  src=""
                  alt="paymentSS"
                  class="payment_screenshot"
                />
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <script type="text/javascript">
      const pending_text = document.querySelector(".pending_table");
      const completed_text = document.querySelector(".completed_table");
      const modalButtons = document.getElementsByClassName("modalButton");
      const tableBody = document.getElementById("tableBody");

      Array.prototype.forEach.call(modalButtons, async function (modalButton) {
        const removeElements = document.getElementById("removeElements");
        removeElements.addEventListener("click", () => {
          tableBody.innerHTML = "";
        });

        modalButton.addEventListener("click", async function (e) {
          const num = parseInt(modalButton.dataset.number);
          const payments = await JSON.parse(modalButton.dataset.payment);
          const imageData = modalButton.dataset.image;
          const paymentDetails = await JSON.parse(
            modalButton.dataset.paymentdetails
          );

          const paymentProof = document.getElementById("paymentProof");
          paymentProof.src = `data:image/image/png;base64,${imageData}`;
          const totalCP = document.getElementById("totalCP");
          totalCP.innerHTML = payments[num].costPrice;

          for (let i = 0; i < paymentDetails[num].length; i++) {
            const tr = document.createElement("tr");

            const rowNumber = document.createElement("th");
            const rowNumberData = document.createTextNode(i + 1);
            rowNumber.append(rowNumberData);
            tr.append(rowNumber);

            const productName = document.createElement("td");
            const productData = document.createTextNode(
              paymentDetails[num][i].productName
            );
            productName.append(productData);
            tr.append(productName);

            const orderTime = document.createElement("td");
            const orderTimeData = document.createTextNode(
              paymentDetails[num][i].orderTime
            );
            orderTime.append(orderTimeData);
            tr.append(orderTime);

            const costPrice = document.createElement("td");
            const costPriceData = document.createTextNode(
              paymentDetails[num][i].costPrice
            );
            costPrice.append(costPriceData);
            tr.append(costPrice);

            const quantity = document.createElement("td");
            const quantityData = document.createTextNode(
              paymentDetails[num][i].quantity
            );
            quantity.append(quantityData);
            tr.append(quantity);
            console.log(tr);
            tableBody.append(tr);
          }
        });
      });

      const select_menu = document.querySelector("#payment_selector");
      select_menu.addEventListener("change", () => {
        pending_text.classList.toggle("d-none");
        completed_text.classList.toggle("d-none");
      });
    </script>
    <script
      src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"
      integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.min.js"
      integrity="sha384-Atwg2Pkwv9vp0ygtn1JAojH0nYbwNJLPhwyoVbhoPwBhjQPR5VtM2+xf0Uwh9KtT"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
